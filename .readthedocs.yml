# .readthedocs.yml
version: 2

sphinx:
  configuration: docs/source/conf.py

build:
  os: ubuntu-22.04

python:
  version: 3.9
  install:
    - method: pip
      path: .
    - requirements: docs/requirements.txt
